// Can also be included with a regular script tag
import Typed from 'typed.js';

var options = {
  strings: ['Félix', 'Félix', 'an intern', 'a developer'],
  typeSpeed: 100,
  loop: 40
};

var typed = new Typed('.element', options);
